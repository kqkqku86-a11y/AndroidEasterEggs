name: Auto Merge Dependabot PRs

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    runs-on: ubuntu-latest

    steps:
      - name: Auto merge Dependabot PRs
        run: |
          for pr in $(gh pr list --author "dependabot[bot]" --state open --json url -q '.[].url'); do
            echo "Merging $pr"
            gh pr merge "$pr" --auto --merge
          done
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
